apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: hipster-shop-daily
  namespace: velero
spec:
  schedule: "0 2 * * *"  # Daily at 2 AM
  template:
    includedNamespaces:
    - hipster-shop
    ttl: 720h0m0s  # 30 days retention
    storageLocation: default
